<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-gray-800">Painel do Médico - {{ $medico->nome }}</h2></x-slot><div class="py-6 max-w-7xl mx-auto space-y-6"><div class="bg-white p-4 shadow rounded"><h3 class="font-semibold mb-2">Agenda e Agendamentos</h3><ul class="text-sm">@foreach($agendamentos as $a)<li>{{ $a->data_hora }} - {{ $a->paciente->nome }} ({{ $a->status }})</li>@endforeach</ul></div><div class="bg-white p-4 shadow rounded"><h3 class="font-semibold mb-2">Prontuário</h3><form method="POST" action="{{ route('medico.prontuario.salvar') }}" class="space-y-2">@csrf <select name="agendamento_id" class="w-full">@foreach($agendamentos as $a)<option value="{{ $a->id }}">{{ $a->paciente->nome }} - {{ $a->data_hora }}</option>@endforeach</select><textarea name="queixa_principal" placeholder="Queixa principal" class="w-full"></textarea><textarea name="historico" placeholder="Histórico" class="w-full"></textarea><input name="sinais_vitais" placeholder="Sinais vitais" class="w-full"/><textarea name="diagnostico" placeholder="Diagnóstico" class="w-full"></textarea><textarea name="conduta" placeholder="Conduta" class="w-full"></textarea><textarea name="observacoes" placeholder="Observações" class="w-full"></textarea><button class="px-4 py-2 bg-blue-600 text-white">Salvar prontuário</button></form></div><div class="bg-white p-4 shadow rounded"><h3 class="font-semibold mb-2">Anamnese e Prescrição</h3><form method="POST" action="{{ route('medico.anamnese-prescricao.salvar') }}" class="space-y-2">@csrf <select name="agendamento_id" class="w-full">@foreach($agendamentos as $a)<option value="{{ $a->id }}">{{ $a->paciente->nome }} - {{ $a->data_hora }}</option>@endforeach</select><textarea name="dados" placeholder="Formulário de anamnese" class="w-full"></textarea><textarea name="medicamentos" placeholder="Medicamentos prescritos" class="w-full"></textarea><button class="px-4 py-2 bg-green-600 text-white">Salvar</button></form></div><div class="bg-white p-4 shadow rounded"><h3 class="font-semibold mb-2">Solicitar Exame (DCM4CHEE HL7/MLLP)</h3><form method="POST" action="{{ route('medico.exame.solicitar') }}" class="space-y-2">@csrf <select name="agendamento_id" class="w-full">@foreach($agendamentos as $a)<option value="{{ $a->id }}">{{ $a->paciente->nome }} - {{ $a->data_hora }}</option>@endforeach</select><select name="exame_id" class="w-full">@foreach($exames as $e)<option value="{{ $e->id }}">{{ $e->codigo }} - {{ $e->descricao }}</option>@endforeach</select><input type="datetime-local" name="agendado_para" class="w-full"/><button class="px-4 py-2 bg-purple-600 text-white">Enviar solicitação</button></form><h4 class="mt-3 font-semibold">Solicitações</h4><ul class="text-sm">@foreach($solicitacoes as $s)<li>{{ $s->numero_pedido }} - {{ $s->status }}</li>@endforeach</ul></div></div></x-app-layout>
